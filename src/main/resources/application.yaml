spring:
  application:
    name: xyz-auth-service
  datasource:
    url: jdbc:mariadb://localhost:3306/auth_db
    username: auth_user
    password: password
    driver-class-name: org.mariadb.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

authservice:
  secret-key: cfb8cde0e80cff0b7052722a8216cbfee9be66c54fa94d8a69f23f31d147fb96368c9c2a3f98123c7968ed74122f53d3e20681ec412f3a72bed6096f648460a3

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MariaDB
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect

---
spring:
  config:
    activate:
      on-profile: test-containers
  datasource:
    url: jdbc:tc:mariadb:11.2:///auth_db
    username: auth_user
    password: password
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

logging:
  level:
    org.testcontainers: INFO
    org.flywaydb: DEBUG
